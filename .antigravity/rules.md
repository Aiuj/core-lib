# Agent Execution Rules

- Always run Python execution commands, scripts, tools, and tests using `uv run` (e.g., `uv run python <file>` or `uv run pytest`) instead of using `.venv\Scripts\python.exe` or activating the virtual environment directly.
